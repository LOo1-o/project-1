# Проверено на двух окружениях: все тесты проходят, итоговые документы
# обоих бюллетеней совпадают ячейка в ячейку.
#   Python 3.11 — pandas 3.0.6, numpy 2.4.6,  openpyxl 3.1.5, python-docx 1.2.0
#   Python 3.8  — pandas 2.0.3, numpy 1.24.4, openpyxl 3.1.5, python-docx 1.1.2
#                 (последние версии, которые ставятся на Windows 7;
#                  точный набор — в requirements-win7.txt)
#
# Верхние границы — не перестраховка: генератор column_mapping_v2 уже
# молча ломался от смены версии pandas (одна и та же строка кода давала
# разный маппинг на pandas 2 и 3). Новую версию сначала проверить на обоих
# бюллетенях, потом поднимать границу.
pandas>=2.0.3,<3.1
numpy>=1.24,<2.5
openpyxl>=3.1,<3.2
python-docx>=1.1,<1.3
