# Точные версии для Windows 7: Python 3.8 — последний Python, который на
# ней работает, и это последние версии библиотек, которые на него ставятся.
# С этим набором проверено: все тесты проходят, маппинг генерируется
# побайтово таким же, как на pandas 3, итоговые документы обоих бюллетеней
# совпадают ячейка в ячейку.
#
#   python -m pip install -r requirements-win7.txt
et-xmlfile==2.0.0
lxml==6.1.3
numpy==1.24.4
openpyxl==3.1.5
pandas==2.0.3
python-dateutil==2.9.0.post0
python-docx==1.1.2
pytz==2026.3.post1
six==1.17.0
typing-extensions==4.13.2
tzdata==2026.4
