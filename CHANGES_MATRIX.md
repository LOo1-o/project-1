# 📍 МАТРИЦА ИЗМЕНЕНИЙ - СТАБИЛИЗАЦИЯ АЛГОРИТМА

## Файлы, требующие изменения (по приоритету)

### 🔴 КРИТИЧНЫЕ ИЗМЕНЕНИЯ

#### 1. `logic.py` - Определение источника таблиц

| Строка | Функция | Проблема | Решение |
|--------|---------|----------|---------|
| ~234-238 | `get_table_source_by_number()` | Зависит от порядка в словаре | Использовать реестр таблиц (TableRegistry) |
| ~230-300 | `generate_word_template()` | Нечеткая логика приоритета источника | Реорганизовать с явной иерархией |
| ~60-80 | `_find_header_rows()` | Жесткий критерий `year_count >= 2` | Адаптивные критерии |

**Файл для создания:** `table_registry.py` (см. IMPLEMENTATION_STAGE1.md)

---

#### 2. `data_filler_v2.py` - Поиск колонок в Excel

| Строка | Функция | Проблема | Решение |
|--------|---------|----------|---------|
| ~150-180 | `_find_column_smart()` | Приоритет жесткого индекса над ключевыми словами | Инвертировать приоритет + валидация |

**Изменение:**
- Сначала поиск по ключевым словам
- Затем жесткий индекс (только если нет ключевых слов)
- Валидировать найденные данные (check for numeric values)

---

#### 3. `config.py` - Поиск названий таблиц

| Строка | Функция | Проблема | Решение |
|--------|---------|----------|---------|
| ~171-260 | `get_table_name()` | Нечеткая нормализация | Добавить fuzzy matching |

**Изменение:**
```python
def get_table_name(table, known_table_names, fuzzy_threshold=0.85):
    # 1. Точное совпадение
    # 2. Нечеткое совпадение (difflib.SequenceMatcher)
    # 3. Логирование уровня совпадения
```

---

### 🟠 ВАЖНЫЕ ДОБАВЛЕНИЯ

#### 4. `validation.py` (НОВЫЙ ФАЙЛ) - Валидация маппингов

**Что добавить:**
```python
def validate_mappings(...)
    # Проверка файлов Excel
    # Проверка дублирования индикаторов
    # Проверка консистентности маппингов
    # Вывод отчета о проблемах

def validate_template_against_mappings(...)
    # Проверка наличия всех таблиц в шаблоне Word
    # Сверка названий таблиц с маппингом
    # Логирование найденных таблиц
```

---

#### 5. `main.py` - Запуск валидации

| Строка | Изменение |
|--------|-----------|
| Шаг 0 (новый) | Добавить `validate_mappings()` перед ШАГ 1 |
| Шаг 1 | Добавить параметр `debug=False` |
| Шаг 2 | Передать `debug` в `generate_word_template()` |

**Добавить параметры:**
```python
def main(input_template_override=None, output_file_override=None, 
         debug=False, debug_table=None):
    """
    debug: выводить подробные логи
    debug_table: проанализировать только одну таблицу (номер)
    """
```

---

### 🟢 РЕКОМЕНДУЕМЫЕ ДОБАВЛЕНИЯ

#### 6. Логирование

**Добавить в каждую функцию:**
```python
if debug:
    print(f"DEBUG: [function_name] step description")
```

---

## 🎯 ПОРЯДОК ВНЕДРЕНИЯ

### Этап 1 (2-3 часа): Критичные проблемы
1. ✅ Создать `table_registry.py`
2. ✅ Интегрировать `TableRegistry` в `generate_word_template()`
3. ✅ Заменить `get_table_source_by_number()` на метод реестра

### Этап 2 (2-3 часа): Адаптивность
1. ✅ Обновить `_find_header_rows()` с адаптивными критериями
2. ✅ Добавить fuzzy matching в `get_table_name()`
3. ✅ Логирование уровня совпадения

### Этап 3 (2-3 часа): Валидация
1. ✅ Создать `validation.py`
2. ✅ Интегрировать валидацию в `main.py`
3. ✅ Добавить DEBUG режим

### Этап 4 (1-2 часа): Заполнение данных
1. ✅ Переделать `_find_column_smart()` в `data_filler_v2.py`
2. ✅ Приоритизировать поиск по ключевым словам
3. ✅ Валидировать найденные данные

---

## 🔍 ДИАГНОСТИЧЕСКИЙ ЧЕКПОИНТ

После каждого этапа проверять:

```bash
# Шаг 1: Создать тестовые файлы
cp input/mappings/table_source_data_mapping.csv \
   input/mappings/table_source_data_mapping_TEST.csv

# Отредактировать TEST версию (добавить/удалить таблицу)

# Шаг 2: Запустить в DEBUG режиме
python main.py --debug --debug-mapping input/mappings/table_source_data_mapping_TEST.csv

# Ожидать:
# ✓ Все таблицы найдены
# ✓ Все источники определены правильно
# ✓ Все теги вставлены
```

---

## ⚠️ РИСКИ И СМЯГЧЕНИЕ

| Риск | Вероятность | Смягчение |
|------|------------|----------|
| Нарушение совместимости | Средняя | Полный unit-test перед деплоем |
| Отсутствие регрессии | Высокая | Сохранить старый код в ветке |
| Невидимые ошибки | Высокая | Добавить DEBUG логи в критичные места |

---

## 📋 КОНТРОЛЬНЫЙ СПИСОК

- [ ] Прочитать STABILITY_ANALYSIS.md
- [ ] Прочитать IMPLEMENTATION_STAGE1.md
- [ ] Создать `table_registry.py`
- [ ] Обновить `logic.py`
- [ ] Обновить `data_filler_v2.py`
- [ ] Создать `validation.py`
- [ ] Обновить `main.py`
- [ ] Написать unit-тесты для новых функций
- [ ] Протестировать на исходных маппингах (должно работать как раньше)
- [ ] Протестировать на измененных маппингах (должно работать лучше)
- [ ] Обновить документацию

